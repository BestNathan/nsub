[package]
name = "nsub"
version = "0.1.0"
edition = "2024"

[dependencies]
nsub-core = { path = "../nsub-core" }

clap = { version = "4", features = ["derive"] }
anyhow = "1"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
toml = "0.8"
